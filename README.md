# 「栖匣 · Nestbox」GitHub Pages 发布说明

这是一键发布的最终成品包，包含全部修复后的主站 `index.html` 和“装桌面当 App”所需的 PWA 文件（清单、图标、离线脚本）。

## 这个包里有什么
- `index.html` — 最终成品（时间表格子可填色、链接微信直开、数据本地私密、一键装桌面）
- `sw.js`（站点根目录）— 离线脚本：在支持 Service Worker 的浏览器里，首次联网打开一次后即可离线打开已载入内容
- `pwa/manifest.json` — 安装为 App 的配置
- `pwa/icon-192.png / 512 / 180` — 应用图标

## 为什么用它
- 免费、永久在线、不用备案
- 微信里直接打开
- 可添加到手机/电脑桌面，像 App 一样用
- 数据存在用户自己的浏览器里，各自独立、彼此看不到、不上传后台

## 一次性操作：把它上线到 GitHub Pages（之后更新全由 AI 完成，不花一分钱）

### 第1步：注册免费 GitHub 账号
打开 https://github.com 注册（邮箱即可，免费）。记住用户名。

### 第2步：新建仓库
登录后，右上角「+」→「New repository」。仓库名写 `pingwanxiang`，选 **Public（公开）**，然后 Create repository。

### 第3步：关闭 Pages 的自动 GitHub Actions 干扰（可选但推荐）
仓库页面 → Settings → Pages，先不动；后续发布方式选「Deploy from a branch」。

### 第4步：把权限交给 AI，让 AI 替你发布并持续更新
在 GitHub：右上头像 → Settings → 最下面 Developer settings → Personal access tokens → Tokens (classic) → Generate new token。
- 勾选 `repo` 权限
- 到期时间建议设长一点（如 1 年）
- 生成后复制 token

把这个 token 和你的 GitHub 用户名、仓库名 `pingwanxiang` 告诉执行 AI，AI 会把包推送上去并开启 Pages，之后每次都自己更新、自动发布你的网址。

> 站点上线地址即为：`https://你的用户名.github.io/pingwanxiang/`
> 由于用的是相对路径（./ 和 pwa/），放在子目录也完全可用。

## 数据安全与“更新不丢记录”
- 用户填写的数据存在各自浏览器本地，绑定在“这个网址”上。只要网址不变，更新网页不会动任何人的记录。
- 各人只见自己的数据，无后台、无上传，隐私有保障。

## 版权与许可
本项目为原创作品，作者：尹平平。以 `LICENSE`（CC BY-NC-ND 4.0 署名—非商业性使用—禁止演绎）发布。你可以非商用分享并署名，但不可商用、不可改动后再发布。涉及就业/保研/考试/政策等时效性内容以官方最新发布为准。

## 以后如何更新
想改任何内容，直接告诉 AI 即可：AI 改好后自动推送发布，你无需再登录 GitHub、不花积分。

## 2026-08 本次改造 · 修改清单
- 全局视觉升级为「暖木匣纸」配色体系：主色核桃木 `#6B4D33`、背景温润陈年米纸 `#F6F2E9`、亮点点缀老黄铜 `#B38546`、柔和边框陶土浅棕 `#E5DBC8`；功能卡片统一干净白底。落地为 `:root` CSS 全局变量，浅/深色模式各一套独立色板（无滤镜反色），全站弹窗、卡片、导航、导览色彩统一。
- 更新日志板块整改：日志只收录用户可感知的功能变化、视觉优化与问题修复，统一「日期｜标签｜标题」格式，按时间倒序展示，页面增加引导小字。
- 全维度体验优化（v79·体验/交互/数据/无障碍）：
  - 导航：子页顶部可点击面包屑；侧栏板块折叠动画+状态记忆；「我的收藏」快捷板块（刷新保留）；侧栏内部独立滚动。
  - 首页：突出「今日核心」为首要焦点；低优先级附加卡可折叠并记忆；待办/打卡空白友好提示。
  - 引导：仅一次「欢迎来到栖匣」首访引导，与年级向导错峰，关闭后永久不弹。
  - 数据：备份自动命名「栖匣-备份-日期.json」；导入二次确认；存储容量临近上限预警；保存失败文字反馈；打卡/倒数日/笔记删除二次确认。
  - 主题：浅深色全站跟随+状态记忆；打印自动适配浅色；无白屏闪动。
  - 阅读/无障碍：全局字号行距统一+留白；长文回到顶部；外链新标签页安全打开+hover反馈；滚动条美化；aria 与键盘可达。
  - 可视化/联动：成长档案近14天轨迹图；历史打卡回看；全站内容检索（关键词高亮）；长文本草稿自动保存。
- 内容与底线：全站教育培训、竞赛、政策资讯保留免责小字；论文学术不端、副业兼职防骗、理财科普高危板块补充温和提示；「免费与边界」说明柔和放置于「关于栖匣」页深处；未改任何业务文本、备考干货与 LocalStorage 存取结构，旧版本地数据升级后 100% 兼容。
- 新手导览重制：从全屏超长滚动页改为 4 步分页交互（品牌 / 适用人群 / 上手小事 / 导航指引），统一暖木色板；首次打开自动弹出，跳过或完成后不再自动弹出，设置页提供「重新观看导览」手动复看。
- 图标规范化：多个位置的彩色 Emoji 替换为内联圆角线性 SVG（描边 2px、颜色跟随主题），全离线、无外部图标 CDN。
- 顶栏新增全站离线搜索框：纯本地不联网，输入关键词实时过滤下拉、回车直达板块；侧边栏搜索同步保留。
- 设置与数据安全：支持「导出全部 JSON 备份」「导入备份恢复」、本地数据完整性自检、一键清空个人记录（二次确认）、浅色/深色模式切换与重新观看导览。
- 移动端适配：宽表格容器自动横向滑动，并在确实溢出的表格上方追加「左右滑动查看完整表格」小字；导航跳转已设 `scroll-margin-top`，标题不被顶部导航遮挡；隐私提示卡片点击「知道了」后永久收起。
- 内容与底线：全站教育培训、竞赛、政策资讯保留免责小字；论文学术不端、副业兼职防骗、理财科普高危板块补充温和提示；「免费与边界」说明柔和放置于「关于栖匣」页深处；未改任何业务文本、备考干货与 LocalStorage 存取结构，旧版本地数据升级后 100% 兼容。
- Service Worker 缓存版本同步提升，保证上线后取到最新内容。