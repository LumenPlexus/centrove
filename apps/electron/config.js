/* 栖匣 · 桌面 App 配置文件
   ----------------------------------------------------------------------------
   ① WEB_URL：线上部署好的栖匣网站地址（必需）。改成你的网址后，
     桌面 App 启动即加载它，网站内容更新时 App 打开永远是最新版。
   ② SYNC_SERVER：多端同步服务端地址（可选，可在 App 内同步面板里再设）。
     留空则只在 App 内手动配置。
   ========================================================================== */
'use strict';

module.exports = {
  // ★★★ 改成你的线上地址，例如 'https://your-domain.com/qi-xia/'
  WEB_URL: 'https://YOUR-HOST/qi-xia/',

  // ★ 自托管同步服务端根地址，例如 'https://your-domain.com/pp-sync'；留空则 App 内配置
  SYNC_SERVER: ''
};